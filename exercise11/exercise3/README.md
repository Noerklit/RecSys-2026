## Environment commands
To update environment:
```
conda env update -f environment.yml --prune
```

To activate command in terminal:
```
conda activate recsys_env
```

Simulator is started on localhost:5002
